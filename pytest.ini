# Restrict collection to tests/: several test_*.py scripts elsewhere in
# the repository (sdradi/, sdradi/pluto_test/, rf_stream/) require SDR
# hardware or TensorFlow and fail at collection time.
[pytest]
testpaths = tests
