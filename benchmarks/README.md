# AIRadar detection benchmark

**Two studies live in this directory, and they answer different questions.** The first
one, below, asks which of the CFAR detectors the repository ships detects best, and
finds no difference to report: at a common measured false-alarm rate the three detect the
target in the same frames 220 times out of 224, so the ranking they appear to have at
their shipped settings is an artefact of three different thresholds rather than a property
of the detectors. That is a statement about this scene -- homogeneous noise is where CA,
GO and SO coincide by construction -- not a general equivalence claim. The second one, "[what actually moves the detection
curve](#second-study-what-actually-moves-the-detection-curve)", starts from that null
result and measures which knobs *do* move it, on the same equal-false-alarm footing.
Each is produced by its own command, and neither hand-types a number into this file.

---

# First study: which of the shipped CFAR detectors detects best

A reproducible, CPU-only, torch-free benchmark for the **classical CFAR detectors that
already ship in this repository**. It exists because the repo contains a large amount of
radar simulation, detection and model code but has never published a single measured
detection number, and the one script that attempted a comparison
(`AIRadar/AIradar_model_comparison.py`) cannot be imported at all: it does
`from AIradar_transformer import RadarTransformerNet` and
`from AImodels_joint import RadarNet, RadarTimeToFreqNet`. `AIradar_transformer` does not
exist anywhere in the repository. `AImodels_joint` does exist, at
`deeplearning/AImodels_joint.py`, but the script puts the repository root on `sys.path`
rather than `deeplearning/`, so it is not importable from there either -- and it defines
neither `RadarNet` nor `RadarTimeToFreqNet`. Of the three classes, `RadarTransformerNet`
and `RadarNet` do exist in `AIRadarLib` (`modeling_transformer.py:11`,
`modeling_RadarNet.py:30`); `RadarTimeToFreqNet` exists only inside training scripts such
as `AIradar_train.py:296`, not in the library. That failure is independent of torch.

Everything below is generated by one command. The results table in this file is written
by the benchmark itself, not typed by hand.

```
python -m benchmarks.run_benchmark --update-readme
```

## What is measured

Three detectors, all repo code, called unmodified:

| name | source | what it is |
|---|---|---|
| `cfar_numpy_go` | `AIRadar/AIRadarLib/radar_det.py::cfar_2d_numpy` | GO-CFAR on the **dB-magnitude** map. The threshold offset is hard-coded at +12 dB; the function has no `Pfa` argument. |
| `cfar_advanced_go` | `AIRadar/AIRadarLib/radar_det.py::cfar_2d_advanced` | GO-CFAR on **linear power** with the threshold multiplier derived from a requested `Pfa`, plus connected-component pruning and the shipped `min_snr_db=6` post-filter. |
| `cfar_custom_datasetv8` | `AIRadar/AIradar_datasetv8.py::AIRadarDataset._cfar_2d_custom` | The detector the dataset pipeline actually calls. GO-CFAR on the dB map with the config's `threshold_offset` (15 dB for the configs used here). |

Scenes come from the repo's own FMCW model, also called unmodified:
`AIRadarDataset.generate_targets`, `_generate_clutter_targets`, `_generate_coupling_target`,
`simulate_fmcw_signal` and `create_target_mask`.

Reported per (detector, scenario, SNR point):

* **Pd** — probability of detection: total true positives / total ground-truth targets.
* **FA/frame** — total false positives / number of frames.
* **FA rate /cell** — total false positives / (frames x eligible cells). "Eligible" mirrors
  each detector's own gating (`range_gate_low < range < R_max`, `|velocity| < v_max`), so the
  denominator is honest. This counts **post-NMS peaks**, not raw threshold crossings, and is
  therefore *not* comparable to a CFAR design `Pfa`.
* **range RMSE / velocity RMSE** — over matched pairs only, against each target's
  *continuous* truth (not its bin centre). Undefined (`n/a`) when nothing matched.
* **peak SNR** — measured mean target-peak-minus-noise-floor in the range-Doppler map, where
  the noise floor is the median dB level of cells at least 6 bins away from every target.
  See "the SNR axis" below for why this column matters.

## The association rule

A detection is matched to a ground-truth target iff **both** hold:

* `|detection.range_bin - target.range_bin| <= gate_range_bins` (default **2** range bins)
* `|detection.doppler_bin - target.doppler_bin| <= gate_doppler_bins` (default **1** Doppler bin)

where the target's bins are its nearest-bin quantization onto `range_axis` / `velocity_axis` —
the same rule `AIRadarDataset.create_target_mask` uses, verified by a unit test.

Matching is **one-to-one and greedy**: every candidate pair inside the gate is scored by the
normalized gate distance `sqrt((dr/Gr)^2 + (dd/Gd)^2)`, pairs are sorted by
`(distance, target_index, detection_index)`, and accepted while both endpoints are still free.
A target can claim at most one detection; every other detection in its gate counts as a false
positive. Unmatched targets are false negatives.

Why bins and not metres: with `zero_pad_factor=2` the range FFT grid is oversampled, so the
range **bin spacing** is `c/(4B)` while the true (Rayleigh) range **resolution** is `c/(2B)`.
The default gate of +/-2 range bins is therefore exactly one true resolution cell. Gating in
bins keeps that relationship explicit and avoids the dimensionally inconsistent
`sqrt(metres^2 + (metres/second)^2)` distance used by
`AIRadarDataset._evaluate_metrics` (see "Repo issues found" below).

Both gates are CLI flags (`--gate-range-bins`, `--gate-doppler-bins`) and their values are
recorded in `results/results.json`, so a different choice is reproducible rather than hidden.

## The SNR axis (read this before interpreting Pd)

The `snr_db` argument of `simulate_fmcw_signal` is a **time-domain** SNR: the noise variance is
set relative to the mean power of the *windowed beat signal*, before any FFT. Coherent range +
Doppler integration over `Ns x Nc` samples then adds tens of dB before the detector sees the
map. For `config_phaser` (1000 samples x 64 chirps) the coherent gain is
`10*log10(1000*64) = 48 dB` less about 1.76 dB of Hann window loss per axis, i.e. **~45 dB**,
which is what the measured `peak SNR` column shows. An input SNR of -20 dB therefore
corresponds to a ~25 dB peak in the range-Doppler map, where every detector saturates at
Pd = 1.

That is why the sweep runs from -50 dB to -15 dB rather than over "sensible" positive SNRs, and
why the measured `peak SNR` column is reported alongside: it is the number that actually
predicts detection. A sweep over positive input SNR would produce Pd = 1.000 everywhere and
say nothing.

## The scenario axis

| scenario | settings | meaning |
|---|---|---|
| `clutter_off` | `apply_realistic_effects=False` | single point target plus AWGN |
| `clutter_strong` | `apply_realistic_effects=True`, `clutter_intensity=1e4` | clutter RCS raised by +40 dB |

An earlier revision of this benchmark also carried a `clutter_default`
(`apply_realistic_effects=True`, `clutter_intensity=1.0`) axis. It was dropped because it
measured nothing: across the full 24-row comparison, **every Pd, TP, FN, range RMSE and
velocity RMSE value was identical to `clutter_off`**, with the only differences being the
mean peak SNR (0.001-0.017 dB) and a single false-positive count. Enabling clutter at that
intensity perturbs the range-Doppler map by about 0.03 dB on average, so it is effectively
a no-op for detection metrics and only doubled the size of every table.

The reason is a measured RCS mismatch: at `clutter_intensity=1.0`,
`_generate_clutter_targets` draws RCS in -50..-20 dBsm while `generate_targets` draws
primary-target RCS in +5..+30 dBsm, for a clutter-to-target RCS power ratio of **-24.5 dB**.
Note that 1.0 is the `AIRadarDataset` class default, *not* the value the repository's own
pipeline uses: the dataset configurations in `AIRadar/AIradar_datasetv8.py` build with
`clutter_intensity=0.1` (three configs) and `0.3` (the CN0566 config), i.e. weaker still.
Scaling the clutter RCS by +40 dB (`clutter_strong`, measured **+15.5 dB** clutter-to-target)
is what finally moves the curves. The exact measured ratio for each scenario is in the
generated table and in `results/results.json`.

## Results

<!-- BEGIN GENERATED RESULTS -->

Radar config `config_phaser` (Phaser_10GHz_DevKit): 500 MHz BW, 64 chirps x 1000 samples, RD map 64 x 667 bins, range bin 0.150 m, velocity bin 0.468 m/s.

16 frames per SNR point, 1 target(s) per frame, base seed 20260819, SNR referenced to **target** power. Association gate: +/-2 range bins (0.300 m), +/-1 Doppler bins (0.468 m/s). Quantization RMSE floor: 0.043 m / 0.135 m/s.

### Stage 1: threshold calibration (threshold -> measured Pfa)

16 target-free frames per scene kind. Threshold axis: dB above the local mean noise power; converted to each detector's own knob by the closed forms in benchmarks/detectors.py. Grid: 6, 7, 8, 9, 10, 11, 12, 13 dB.

* target Pfa 1e-03: 42240 eligible cells x 16 frames = 675,840 cell trials -> 675.8 expected false alarms, **measurable** (10 events needs 1 frames).
* target Pfa 1e-04: 42240 eligible cells x 16 frames = 675,840 cell trials -> 67.6 expected false alarms, **measurable** (10 events needs 3 frames).

Scene kind `noise_only` (source scenario `clutter_off`):

| detector | knob | threshold (dB) | knob value | false alarms | measured Pfa /cell |
|---|---|---|---|---|---|
| `cfar_numpy_go` | `magnitude_warp_exponent` | 6.00 | 1.41063 | 8132 | 1.20e-02 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 7.00 | 1.26225 | 3335 | 4.93e-03 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 8.00 | 1.14212 | 1010 | 1.49e-03 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 9.00 | 1.04286 | 185 | 2.74e-04 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 9.49 (as shipped) | 1 | 66 | 9.77e-05 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 10.00 | 0.959477 | 24 | 3.55e-05 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 11.00 | 0.88844 | 1 | 1.48e-06 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 12.00 | 0.827197 | 0 | 0 (none observed) |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 13.00 | 0.773853 | 0 | 0 (none observed) |
| `cfar_advanced_go` | `pfa` | 6.00 | 0.368196 | 8267 | 1.22e-02 |
| `cfar_advanced_go` | `pfa` | 7.00 | 0.284347 | 3380 | 5.00e-03 |
| `cfar_advanced_go` | `pfa` | 8.00 | 0.205413 | 1006 | 1.49e-03 |
| `cfar_advanced_go` | `pfa` | 9.00 | 0.136444 | 188 | 2.78e-04 |
| `cfar_advanced_go` | `pfa` | 10.00 | 0.0815562 | 20 | 2.96e-05 |
| `cfar_advanced_go` | `pfa` | 11.00 | 0.0426944 | 1 | 1.48e-06 |
| `cfar_advanced_go` | `pfa` | 12.00 | 0.0189212 | 0 | 0 (none observed) |
| `cfar_advanced_go` | `pfa` | 13.00 | 0.00680334 | 0 | 0 (none observed) |
| `cfar_advanced_go` | `pfa` | 16.66 (as shipped) | 1e-05 | 0 | 0 (none observed) |
| `cfar_custom_datasetv8` | `threshold_offset` | 6.00 | 8.50682 | 8164 | 1.20e-02 |
| `cfar_custom_datasetv8` | `threshold_offset` | 7.00 | 9.50682 | 3351 | 4.94e-03 |
| `cfar_custom_datasetv8` | `threshold_offset` | 8.00 | 10.5068 | 1015 | 1.50e-03 |
| `cfar_custom_datasetv8` | `threshold_offset` | 9.00 | 11.5068 | 186 | 2.74e-04 |
| `cfar_custom_datasetv8` | `threshold_offset` | 10.00 | 12.5068 | 25 | 3.68e-05 |
| `cfar_custom_datasetv8` | `threshold_offset` | 11.00 | 13.5068 | 1 | 1.47e-06 |
| `cfar_custom_datasetv8` | `threshold_offset` | 12.00 | 14.5068 | 0 | 0 (none observed) |
| `cfar_custom_datasetv8` | `threshold_offset` | 12.49 (as shipped) | 15 | 0 | 0 (none observed) |
| `cfar_custom_datasetv8` | `threshold_offset` | 13.00 | 15.5068 | 0 | 0 (none observed) |

Scene kind `clutter_only` (source scenario `clutter_strong`):

| detector | knob | threshold (dB) | knob value | false alarms | measured Pfa /cell |
|---|---|---|---|---|---|
| `cfar_numpy_go` | `magnitude_warp_exponent` | 6.00 | 1.41063 | 8105 | 1.20e-02 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 7.00 | 1.26225 | 3364 | 4.98e-03 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 8.00 | 1.14212 | 1075 | 1.59e-03 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 9.00 | 1.04286 | 255 | 3.77e-04 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 9.49 (as shipped) | 1 | 131 | 1.94e-04 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 10.00 | 0.959477 | 84 | 1.24e-04 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 11.00 | 0.88844 | 55 | 8.14e-05 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 12.00 | 0.827197 | 43 | 6.36e-05 |
| `cfar_numpy_go` | `magnitude_warp_exponent` | 13.00 | 0.773853 | 37 | 5.47e-05 |
| `cfar_advanced_go` | `pfa` | 6.00 | 0.368196 | 8019 | 1.19e-02 |
| `cfar_advanced_go` | `pfa` | 7.00 | 0.284347 | 3310 | 4.90e-03 |
| `cfar_advanced_go` | `pfa` | 8.00 | 0.205413 | 1045 | 1.55e-03 |
| `cfar_advanced_go` | `pfa` | 9.00 | 0.136444 | 243 | 3.60e-04 |
| `cfar_advanced_go` | `pfa` | 10.00 | 0.0815562 | 76 | 1.12e-04 |
| `cfar_advanced_go` | `pfa` | 11.00 | 0.0426944 | 50 | 7.40e-05 |
| `cfar_advanced_go` | `pfa` | 12.00 | 0.0189212 | 41 | 6.07e-05 |
| `cfar_advanced_go` | `pfa` | 13.00 | 0.00680334 | 32 | 4.73e-05 |
| `cfar_advanced_go` | `pfa` | 16.66 (as shipped) | 1e-05 | 17 | 2.52e-05 |
| `cfar_custom_datasetv8` | `threshold_offset` | 6.00 | 8.50682 | 8134 | 1.20e-02 |
| `cfar_custom_datasetv8` | `threshold_offset` | 7.00 | 9.50682 | 3377 | 4.97e-03 |
| `cfar_custom_datasetv8` | `threshold_offset` | 8.00 | 10.5068 | 1077 | 1.59e-03 |
| `cfar_custom_datasetv8` | `threshold_offset` | 9.00 | 11.5068 | 256 | 3.77e-04 |
| `cfar_custom_datasetv8` | `threshold_offset` | 10.00 | 12.5068 | 85 | 1.25e-04 |
| `cfar_custom_datasetv8` | `threshold_offset` | 11.00 | 13.5068 | 55 | 8.10e-05 |
| `cfar_custom_datasetv8` | `threshold_offset` | 12.00 | 14.5068 | 43 | 6.33e-05 |
| `cfar_custom_datasetv8` | `threshold_offset` | 12.49 (as shipped) | 15 | 40 | 5.89e-05 |
| `cfar_custom_datasetv8` | `threshold_offset` | 13.00 | 15.5068 | 37 | 5.45e-05 |

Solved operating points:

| detector | scene kind | target Pfa | solved threshold (dB) | knob value | status |
|---|---|---|---|---|---|
| `cfar_numpy_go` | noise_only | 1e-03 | 8.237 | 1.116954 | interpolated |
| `cfar_numpy_go` | noise_only | 1e-04 | 9.493 | 1.000010 | interpolated |
| `cfar_advanced_go` | noise_only | 1e-03 | 8.237 | 0.187977 | interpolated |
| `cfar_advanced_go` | noise_only | 1e-04 | 9.457 | 0.109457 | interpolated |
| `cfar_custom_datasetv8` | noise_only | 1e-03 | 8.237 | 10.743809 | interpolated |
| `cfar_custom_datasetv8` | noise_only | 1e-04 | 9.502 | 12.009011 | interpolated |
| `cfar_numpy_go` | clutter_only | 1e-03 | 8.323 | 1.108096 | interpolated |
| `cfar_numpy_go` | clutter_only | 1e-04 | 10.513 | 0.921639 | interpolated |
| `cfar_advanced_go` | clutter_only | 1e-03 | 8.299 | 0.183547 | interpolated |
| `cfar_advanced_go` | clutter_only | 1e-04 | 10.280 | 0.069032 | interpolated |
| `cfar_custom_datasetv8` | clutter_only | 1e-03 | 8.321 | 10.827986 | interpolated |
| `cfar_custom_datasetv8` | clutter_only | 1e-04 | 10.516 | 13.023093 | interpolated |

the sweep uses the noise-only solve. The clutter-only curve is reported beside it: under clutter_strong the clutter scatterers are real peaks, so there is a floor on the achievable peak density that no threshold removes.

### Stage 2a (headline): all detectors at the calibrated common operating point, Pfa = 1e-04 /cell

Thresholds in use: `cfar_advanced_go` pfa=0.109457 (9.46 dB); `cfar_custom_datasetv8` threshold_offset=12.009011 (9.50 dB); `cfar_numpy_go` magnitude_warp_exponent=1.000010 (9.49 dB).

#### `clutter_off` -- `apply_realistic_effects=False`, `clutter_intensity=1` -- clutter disabled; noise referenced to target-only power, which required +0.00 dB mean correction to the requested SNR.

| detector | thr (dB) | SNR in (dB) | target-bin SNR (dB) | Pd | FA/frame | FA rate /cell | range RMSE (m) | vel RMSE (m/s) | TP | FP | FN |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 9.46 | -42 | 2.3 | 0.062 | 5.56 | 1.32e-04 | 0.0603 | 0.3276 | 1 | 89 | 15 |
| `cfar_advanced_go` | 9.46 | -39 | 4.2 | 0.062 | 6.00 | 1.42e-04 | 0.0367 | 0.1470 | 1 | 96 | 15 |
| `cfar_advanced_go` | 9.46 | -36 | 7.3 | 0.250 | 5.12 | 1.21e-04 | 0.0720 | 0.1578 | 4 | 82 | 12 |
| `cfar_advanced_go` | 9.46 | -33 | 10.3 | 0.938 | 5.62 | 1.33e-04 | 0.0904 | 0.1704 | 15 | 90 | 1 |
| `cfar_advanced_go` | 9.46 | -30 | 13.7 | 1.000 | 5.81 | 1.38e-04 | 0.0897 | 0.1678 | 16 | 93 | 0 |
| `cfar_advanced_go` | 9.46 | -27 | 16.9 | 1.000 | 5.62 | 1.33e-04 | 0.0539 | 0.1763 | 16 | 90 | 0 |
| `cfar_advanced_go` | 9.46 | -24 | 19.7 | 1.000 | 5.31 | 1.26e-04 | 0.0651 | 0.1396 | 16 | 85 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -42 | 2.3 | 0.000 | 5.31 | 1.25e-04 | n/a | n/a | 0 | 85 | 16 |
| `cfar_custom_datasetv8` | 9.50 | -39 | 4.2 | 0.062 | 5.69 | 1.34e-04 | 0.0367 | 0.1470 | 1 | 91 | 15 |
| `cfar_custom_datasetv8` | 9.50 | -36 | 7.3 | 0.250 | 4.75 | 1.12e-04 | 0.0720 | 0.1578 | 4 | 76 | 12 |
| `cfar_custom_datasetv8` | 9.50 | -33 | 10.3 | 0.938 | 5.25 | 1.24e-04 | 0.0904 | 0.1704 | 15 | 84 | 1 |
| `cfar_custom_datasetv8` | 9.50 | -30 | 13.7 | 1.000 | 5.44 | 1.28e-04 | 0.0897 | 0.1678 | 16 | 87 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -27 | 16.9 | 1.000 | 5.75 | 1.36e-04 | 0.0539 | 0.1763 | 16 | 92 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -24 | 19.7 | 1.000 | 4.75 | 1.12e-04 | 0.0651 | 0.1396 | 16 | 76 | 0 |
| `cfar_numpy_go` | 9.49 | -42 | 2.3 | 0.000 | 5.31 | 1.26e-04 | n/a | n/a | 0 | 85 | 16 |
| `cfar_numpy_go` | 9.49 | -39 | 4.2 | 0.062 | 5.62 | 1.33e-04 | 0.0367 | 0.1470 | 1 | 90 | 15 |
| `cfar_numpy_go` | 9.49 | -36 | 7.3 | 0.250 | 4.75 | 1.12e-04 | 0.0720 | 0.1578 | 4 | 76 | 12 |
| `cfar_numpy_go` | 9.49 | -33 | 10.3 | 0.938 | 5.50 | 1.30e-04 | 0.0904 | 0.1704 | 15 | 88 | 1 |
| `cfar_numpy_go` | 9.49 | -30 | 13.7 | 1.000 | 5.56 | 1.32e-04 | 0.0897 | 0.1678 | 16 | 89 | 0 |
| `cfar_numpy_go` | 9.49 | -27 | 16.9 | 1.000 | 5.81 | 1.38e-04 | 0.0539 | 0.1763 | 16 | 93 | 0 |
| `cfar_numpy_go` | 9.49 | -24 | 19.7 | 1.000 | 4.88 | 1.15e-04 | 0.0651 | 0.1396 | 16 | 78 | 0 |

#### `clutter_strong` -- `apply_realistic_effects=True`, `clutter_intensity=10000` -- clutter/target RCS power +15.5 dB; noise referenced to target-only power, which required +11.24 dB mean correction to the requested SNR.

| detector | thr (dB) | SNR in (dB) | target-bin SNR (dB) | Pd | FA/frame | FA rate /cell | range RMSE (m) | vel RMSE (m/s) | TP | FP | FN |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 9.46 | -42 | 2.3 | 0.062 | 5.81 | 1.38e-04 | 0.0603 | 0.3276 | 1 | 93 | 15 |
| `cfar_advanced_go` | 9.46 | -39 | 4.2 | 0.062 | 6.81 | 1.61e-04 | 0.0367 | 0.1470 | 1 | 109 | 15 |
| `cfar_advanced_go` | 9.46 | -36 | 7.3 | 0.250 | 6.50 | 1.54e-04 | 0.0720 | 0.1578 | 4 | 104 | 12 |
| `cfar_advanced_go` | 9.46 | -33 | 10.2 | 0.875 | 8.06 | 1.91e-04 | 0.0904 | 0.1723 | 14 | 129 | 2 |
| `cfar_advanced_go` | 9.46 | -30 | 13.5 | 1.000 | 9.44 | 2.23e-04 | 0.0897 | 0.1678 | 16 | 151 | 0 |
| `cfar_advanced_go` | 9.46 | -27 | 16.5 | 1.000 | 11.44 | 2.71e-04 | 0.0539 | 0.1763 | 16 | 183 | 0 |
| `cfar_advanced_go` | 9.46 | -24 | 19.0 | 1.000 | 14.31 | 3.39e-04 | 0.0651 | 0.1396 | 16 | 229 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -42 | 2.3 | 0.000 | 5.75 | 1.36e-04 | n/a | n/a | 0 | 92 | 16 |
| `cfar_custom_datasetv8` | 9.50 | -39 | 4.2 | 0.000 | 6.44 | 1.52e-04 | n/a | n/a | 0 | 103 | 16 |
| `cfar_custom_datasetv8` | 9.50 | -36 | 7.3 | 0.250 | 6.31 | 1.49e-04 | 0.0720 | 0.1578 | 4 | 101 | 12 |
| `cfar_custom_datasetv8` | 9.50 | -33 | 10.2 | 0.938 | 8.00 | 1.89e-04 | 0.0904 | 0.1704 | 15 | 128 | 1 |
| `cfar_custom_datasetv8` | 9.50 | -30 | 13.5 | 1.000 | 9.62 | 2.27e-04 | 0.0897 | 0.1678 | 16 | 154 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -27 | 16.5 | 1.000 | 12.50 | 2.95e-04 | 0.0539 | 0.1763 | 16 | 200 | 0 |
| `cfar_custom_datasetv8` | 9.50 | -24 | 19.0 | 1.000 | 14.81 | 3.49e-04 | 0.0651 | 0.1396 | 16 | 237 | 0 |
| `cfar_numpy_go` | 9.49 | -42 | 2.3 | 0.000 | 5.75 | 1.36e-04 | n/a | n/a | 0 | 92 | 16 |
| `cfar_numpy_go` | 9.49 | -39 | 4.2 | 0.000 | 6.38 | 1.51e-04 | n/a | n/a | 0 | 102 | 16 |
| `cfar_numpy_go` | 9.49 | -36 | 7.3 | 0.250 | 6.31 | 1.49e-04 | 0.0720 | 0.1578 | 4 | 101 | 12 |
| `cfar_numpy_go` | 9.49 | -33 | 10.2 | 0.938 | 8.19 | 1.94e-04 | 0.0904 | 0.1704 | 15 | 131 | 1 |
| `cfar_numpy_go` | 9.49 | -30 | 13.5 | 1.000 | 9.69 | 2.29e-04 | 0.0897 | 0.1678 | 16 | 155 | 0 |
| `cfar_numpy_go` | 9.49 | -27 | 16.5 | 1.000 | 12.62 | 2.99e-04 | 0.0539 | 0.1763 | 16 | 202 | 0 |
| `cfar_numpy_go` | 9.49 | -24 | 19.0 | 1.000 | 15.00 | 3.55e-04 | 0.0651 | 0.1396 | 16 | 240 | 0 |

### Stage 2b: the same sweep with the thresholds as configured in the repo

Kept because it is what a user of this repository actually gets. It is **not** a detector comparison: the three effective thresholds are `cfar_advanced_go` 16.66 dB, `cfar_custom_datasetv8` 12.49 dB, `cfar_numpy_go` 9.49 dB, so the Pd order mostly follows the threshold order.

#### `clutter_off` -- `apply_realistic_effects=False`, `clutter_intensity=1` -- clutter disabled; noise referenced to target-only power, which required +0.00 dB mean correction to the requested SNR.

| detector | thr (dB) | SNR in (dB) | target-bin SNR (dB) | Pd | FA/frame | FA rate /cell | range RMSE (m) | vel RMSE (m/s) | TP | FP | FN |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 16.66 | -42 | 2.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -39 | 4.2 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -36 | 7.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -33 | 10.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -30 | 13.7 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -27 | 16.9 | 0.500 | 0.00 | 0 (none observed) | 0.0637 | 0.1631 | 8 | 0 | 8 |
| `cfar_advanced_go` | 16.66 | -24 | 19.7 | 1.000 | 0.00 | 0 (none observed) | 0.0651 | 0.1396 | 16 | 0 | 0 |
| `cfar_custom_datasetv8` | 12.49 | -42 | 2.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -39 | 4.2 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -36 | 7.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -33 | 10.3 | 0.125 | 0.00 | 0 (none observed) | 0.0880 | 0.0960 | 2 | 0 | 14 |
| `cfar_custom_datasetv8` | 12.49 | -30 | 13.7 | 0.938 | 0.00 | 0 (none observed) | 0.0926 | 0.1667 | 15 | 0 | 1 |
| `cfar_custom_datasetv8` | 12.49 | -27 | 16.9 | 1.000 | 0.00 | 0 (none observed) | 0.0539 | 0.1763 | 16 | 0 | 0 |
| `cfar_custom_datasetv8` | 12.49 | -24 | 19.7 | 1.000 | 0.00 | 0 (none observed) | 0.0651 | 0.1396 | 16 | 0 | 0 |
| `cfar_numpy_go` | 9.49 | -42 | 2.3 | 0.000 | 5.31 | 1.26e-04 | n/a | n/a | 0 | 85 | 16 |
| `cfar_numpy_go` | 9.49 | -39 | 4.2 | 0.062 | 5.62 | 1.33e-04 | 0.0367 | 0.1470 | 1 | 90 | 15 |
| `cfar_numpy_go` | 9.49 | -36 | 7.3 | 0.250 | 4.75 | 1.12e-04 | 0.0720 | 0.1578 | 4 | 76 | 12 |
| `cfar_numpy_go` | 9.49 | -33 | 10.3 | 0.938 | 5.50 | 1.30e-04 | 0.0904 | 0.1704 | 15 | 88 | 1 |
| `cfar_numpy_go` | 9.49 | -30 | 13.7 | 1.000 | 5.56 | 1.32e-04 | 0.0897 | 0.1678 | 16 | 89 | 0 |
| `cfar_numpy_go` | 9.49 | -27 | 16.9 | 1.000 | 5.81 | 1.38e-04 | 0.0539 | 0.1763 | 16 | 93 | 0 |
| `cfar_numpy_go` | 9.49 | -24 | 19.7 | 1.000 | 4.88 | 1.15e-04 | 0.0651 | 0.1396 | 16 | 78 | 0 |

#### `clutter_strong` -- `apply_realistic_effects=True`, `clutter_intensity=10000` -- clutter/target RCS power +15.5 dB; noise referenced to target-only power, which required +11.24 dB mean correction to the requested SNR.

| detector | thr (dB) | SNR in (dB) | target-bin SNR (dB) | Pd | FA/frame | FA rate /cell | range RMSE (m) | vel RMSE (m/s) | TP | FP | FN |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 16.66 | -42 | 2.3 | 0.000 | 0.00 | 0 (none observed) | n/a | n/a | 0 | 0 | 16 |
| `cfar_advanced_go` | 16.66 | -39 | 4.2 | 0.000 | 0.06 | 1.48e-06 | n/a | n/a | 0 | 1 | 16 |
| `cfar_advanced_go` | 16.66 | -36 | 7.3 | 0.000 | 0.31 | 7.40e-06 | n/a | n/a | 0 | 5 | 16 |
| `cfar_advanced_go` | 16.66 | -33 | 10.2 | 0.000 | 0.44 | 1.04e-05 | n/a | n/a | 0 | 7 | 16 |
| `cfar_advanced_go` | 16.66 | -30 | 13.5 | 0.000 | 0.94 | 2.22e-05 | n/a | n/a | 0 | 15 | 16 |
| `cfar_advanced_go` | 16.66 | -27 | 16.5 | 0.500 | 1.81 | 4.29e-05 | 0.0637 | 0.1631 | 8 | 29 | 8 |
| `cfar_advanced_go` | 16.66 | -24 | 19.0 | 1.000 | 2.44 | 5.77e-05 | 0.0651 | 0.1396 | 16 | 39 | 0 |
| `cfar_custom_datasetv8` | 12.49 | -42 | 2.3 | 0.000 | 0.12 | 2.95e-06 | n/a | n/a | 0 | 2 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -39 | 4.2 | 0.000 | 0.38 | 8.84e-06 | n/a | n/a | 0 | 6 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -36 | 7.3 | 0.000 | 0.69 | 1.62e-05 | n/a | n/a | 0 | 11 | 16 |
| `cfar_custom_datasetv8` | 12.49 | -33 | 10.2 | 0.125 | 1.62 | 3.83e-05 | 0.0880 | 0.0960 | 2 | 26 | 14 |
| `cfar_custom_datasetv8` | 12.49 | -30 | 13.5 | 0.875 | 2.56 | 6.04e-05 | 0.0909 | 0.1680 | 14 | 41 | 2 |
| `cfar_custom_datasetv8` | 12.49 | -27 | 16.5 | 1.000 | 3.44 | 8.10e-05 | 0.0539 | 0.1763 | 16 | 55 | 0 |
| `cfar_custom_datasetv8` | 12.49 | -24 | 19.0 | 1.000 | 5.56 | 1.31e-04 | 0.0651 | 0.1396 | 16 | 89 | 0 |
| `cfar_numpy_go` | 9.49 | -42 | 2.3 | 0.000 | 5.75 | 1.36e-04 | n/a | n/a | 0 | 92 | 16 |
| `cfar_numpy_go` | 9.49 | -39 | 4.2 | 0.000 | 6.38 | 1.51e-04 | n/a | n/a | 0 | 102 | 16 |
| `cfar_numpy_go` | 9.49 | -36 | 7.3 | 0.250 | 6.31 | 1.49e-04 | 0.0720 | 0.1578 | 4 | 101 | 12 |
| `cfar_numpy_go` | 9.49 | -33 | 10.2 | 0.938 | 8.19 | 1.94e-04 | 0.0904 | 0.1704 | 15 | 131 | 1 |
| `cfar_numpy_go` | 9.49 | -30 | 13.5 | 1.000 | 9.69 | 2.29e-04 | 0.0897 | 0.1678 | 16 | 155 | 0 |
| `cfar_numpy_go` | 9.49 | -27 | 16.5 | 1.000 | 12.62 | 2.99e-04 | 0.0539 | 0.1763 | 16 | 202 | 0 |
| `cfar_numpy_go` | 9.49 | -24 | 19.0 | 1.000 | 15.00 | 3.55e-04 | 0.0651 | 0.1396 | 16 | 240 | 0 |

### Stage 3: ROC -- Pd vs *measured* noise-only Pfa, scenario `clutter_off`

Each row is one point of the threshold grid. The Pfa column is the density measured on the target-free frames of stage 1, not a design parameter.

At input SNR -33 dB (target-bin SNR 10.3 dB):

| detector | threshold (dB) | knob value | measured Pfa /cell | Pd | FA/frame on target frames | of which near the target |
|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 6.00 | 0.368196 | 1.22e-02 | 1.000 | 516.38 | 0.62 |
| `cfar_advanced_go` | 7.00 | 0.284347 | 5.00e-03 | 1.000 | 212.62 | 0.31 |
| `cfar_advanced_go` | 8.00 | 0.205413 | 1.49e-03 | 1.000 | 61.88 | 0.00 |
| `cfar_advanced_go` | 9.00 | 0.136444 | 2.78e-04 | 1.000 | 12.75 | 0.00 |
| `cfar_advanced_go` | 10.00 | 0.0815562 | 2.96e-05 | 0.938 | 1.88 | 0.00 |
| `cfar_advanced_go` | 11.00 | 0.0426944 | 1.48e-06 | 0.438 | 0.00 | 0.00 |
| `cfar_advanced_go` | 12.00 | 0.0189212 | 0 (none observed) | 0.188 | 0.00 | 0.00 |
| `cfar_advanced_go` | 13.00 | 0.00680334 | 0 (none observed) | 0.062 | 0.00 | 0.00 |
| `cfar_custom_datasetv8` | 6.00 | 8.50682 | 1.20e-02 | 1.000 | 512.06 | 0.69 |
| `cfar_custom_datasetv8` | 7.00 | 9.50682 | 4.94e-03 | 1.000 | 213.38 | 0.19 |
| `cfar_custom_datasetv8` | 8.00 | 10.5068 | 1.50e-03 | 1.000 | 62.56 | 0.12 |
| `cfar_custom_datasetv8` | 9.00 | 11.5068 | 2.74e-04 | 1.000 | 12.62 | 0.00 |
| `cfar_custom_datasetv8` | 10.00 | 12.5068 | 3.68e-05 | 0.938 | 1.88 | 0.00 |
| `cfar_custom_datasetv8` | 11.00 | 13.5068 | 1.47e-06 | 0.438 | 0.06 | 0.00 |
| `cfar_custom_datasetv8` | 12.00 | 14.5068 | 0 (none observed) | 0.188 | 0.00 | 0.00 |
| `cfar_custom_datasetv8` | 13.00 | 15.5068 | 0 (none observed) | 0.062 | 0.00 | 0.00 |
| `cfar_numpy_go` | 6.00 | 1.41063 | 1.20e-02 | 1.000 | 510.00 | 0.69 |
| `cfar_numpy_go` | 7.00 | 1.26225 | 4.93e-03 | 1.000 | 212.62 | 0.19 |
| `cfar_numpy_go` | 8.00 | 1.14212 | 1.49e-03 | 1.000 | 62.38 | 0.12 |
| `cfar_numpy_go` | 9.00 | 1.04286 | 2.74e-04 | 1.000 | 12.56 | 0.00 |
| `cfar_numpy_go` | 10.00 | 0.959477 | 3.55e-05 | 0.938 | 1.88 | 0.00 |
| `cfar_numpy_go` | 11.00 | 0.88844 | 1.48e-06 | 0.438 | 0.06 | 0.00 |
| `cfar_numpy_go` | 12.00 | 0.827197 | 0 (none observed) | 0.188 | 0.00 | 0.00 |
| `cfar_numpy_go` | 13.00 | 0.773853 | 0 (none observed) | 0.062 | 0.00 | 0.00 |

At input SNR -27 dB (target-bin SNR 16.9 dB):

| detector | threshold (dB) | knob value | measured Pfa /cell | Pd | FA/frame on target frames | of which near the target |
|---|---|---|---|---|---|---|
| `cfar_advanced_go` | 6.00 | 0.368196 | 1.22e-02 | 1.000 | 521.31 | 0.25 |
| `cfar_advanced_go` | 7.00 | 0.284347 | 5.00e-03 | 1.000 | 213.19 | 0.00 |
| `cfar_advanced_go` | 8.00 | 0.205413 | 1.49e-03 | 1.000 | 63.69 | 0.00 |
| `cfar_advanced_go` | 9.00 | 0.136444 | 2.78e-04 | 1.000 | 13.25 | 0.00 |
| `cfar_advanced_go` | 10.00 | 0.0815562 | 2.96e-05 | 1.000 | 1.69 | 0.00 |
| `cfar_advanced_go` | 11.00 | 0.0426944 | 1.48e-06 | 1.000 | 0.12 | 0.00 |
| `cfar_advanced_go` | 12.00 | 0.0189212 | 0 (none observed) | 1.000 | 0.00 | 0.00 |
| `cfar_advanced_go` | 13.00 | 0.00680334 | 0 (none observed) | 1.000 | 0.00 | 0.00 |
| `cfar_custom_datasetv8` | 6.00 | 8.50682 | 1.20e-02 | 1.000 | 523.31 | 0.38 |
| `cfar_custom_datasetv8` | 7.00 | 9.50682 | 4.94e-03 | 1.000 | 215.19 | 0.19 |
| `cfar_custom_datasetv8` | 8.00 | 10.5068 | 1.50e-03 | 1.000 | 64.69 | 0.00 |
| `cfar_custom_datasetv8` | 9.00 | 11.5068 | 2.74e-04 | 1.000 | 13.44 | 0.00 |
| `cfar_custom_datasetv8` | 10.00 | 12.5068 | 3.68e-05 | 1.000 | 2.00 | 0.00 |
| `cfar_custom_datasetv8` | 11.00 | 13.5068 | 1.47e-06 | 1.000 | 0.19 | 0.00 |
| `cfar_custom_datasetv8` | 12.00 | 14.5068 | 0 (none observed) | 1.000 | 0.00 | 0.00 |
| `cfar_custom_datasetv8` | 13.00 | 15.5068 | 0 (none observed) | 1.000 | 0.00 | 0.00 |
| `cfar_numpy_go` | 6.00 | 1.41063 | 1.20e-02 | 1.000 | 521.31 | 0.38 |
| `cfar_numpy_go` | 7.00 | 1.26225 | 4.93e-03 | 1.000 | 214.06 | 0.19 |
| `cfar_numpy_go` | 8.00 | 1.14212 | 1.49e-03 | 1.000 | 64.25 | 0.00 |
| `cfar_numpy_go` | 9.00 | 1.04286 | 2.74e-04 | 1.000 | 13.25 | 0.00 |
| `cfar_numpy_go` | 10.00 | 0.959477 | 3.55e-05 | 1.000 | 2.00 | 0.00 |
| `cfar_numpy_go` | 11.00 | 0.88844 | 1.48e-06 | 1.000 | 0.19 | 0.00 |
| `cfar_numpy_go` | 12.00 | 0.827197 | 0 (none observed) | 1.000 | 0.00 | 0.00 |
| `cfar_numpy_go` | 13.00 | 0.773853 | 0 (none observed) | 1.000 | 0.00 | 0.00 |

### How much of the measured false-alarm rate is the target's own sidelobes

Every in-gate detection beyond the one matched pair counts as a false positive, so a target sidelobe that survives non-maximum suppression is reported as a false alarm. Split at the highest SNR point (-24 dB) using a wider +/-8 range bin, +/-3 Doppler bin neighbourhood (which changes no count -- it only labels them):

| detector | variant | scenario | FP total | FP near a target | share | FP/frame far from any target |
|---|---|---|---|---|---|---|
| `cfar_advanced_go` | as_shipped | clutter_off | 0 | 0 | n/a | 0.00 |
| `cfar_advanced_go` | as_shipped | clutter_strong | 39 | 0 | 0.0% | 2.44 |
| `cfar_custom_datasetv8` | as_shipped | clutter_off | 0 | 0 | n/a | 0.00 |
| `cfar_custom_datasetv8` | as_shipped | clutter_strong | 89 | 0 | 0.0% | 5.56 |
| `cfar_numpy_go` | as_shipped | clutter_off | 78 | 0 | 0.0% | 4.88 |
| `cfar_numpy_go` | as_shipped | clutter_strong | 240 | 0 | 0.0% | 15.00 |
| `cfar_advanced_go` | calibrated_pfa1e-4 | clutter_off | 85 | 0 | 0.0% | 5.31 |
| `cfar_advanced_go` | calibrated_pfa1e-4 | clutter_strong | 229 | 0 | 0.0% | 14.31 |
| `cfar_custom_datasetv8` | calibrated_pfa1e-4 | clutter_off | 76 | 0 | 0.0% | 4.75 |
| `cfar_custom_datasetv8` | calibrated_pfa1e-4 | clutter_strong | 237 | 0 | 0.0% | 14.81 |
| `cfar_numpy_go` | calibrated_pfa1e-4 | clutter_off | 78 | 0 | 0.0% | 4.88 |
| `cfar_numpy_go` | calibrated_pfa1e-4 | clutter_strong | 240 | 0 | 0.0% | 15.00 |

Runtime: 1014.5 s wall clock for 224 swept frames plus 16 calibration frames per scene kind, 2976 detector calls (49.6 s simulation, cfar_advanced_go 322.4 s, cfar_custom_datasetv8 177.3 s, cfar_numpy_go 181.5 s).

Environment: Python 3.14.2, numpy 2.5.2, scipy 1.18.0, matplotlib 3.11.1, torch not installed, tqdm not installed, Windows-11-10.0.26200-SP0, device cpu.

Provenance: generated from commit `2a1b4fae3fdd` on branch `research/benchmark-harness`, worktree DIRTY (tracked diff sha256 22a42d4f3fc7). That commit is the parent of the commit carrying this file, because the results are generated before they are committed.

<!-- END GENERATED RESULTS -->

Figures (Agg backend, written only when matplotlib is installed).
Stage 1: `results/calibration_curves.png`. Stage 2, one figure per variant
(`_as_shipped` and `_calibrated`): `results/pd_vs_snr_*.png`,
`results/fa_per_frame_vs_snr_*.png`, `results/range_rmse_vs_snr_*.png`,
`results/velocity_rmse_vs_snr_*.png`. Stage 3:
`results/roc_pd_vs_measured_pfa.png`.

The figures are **not tracked in git** -- they are regenerated by the command above, and a
committed image cannot be checked against the code that made it, so it would keep looking
authoritative after the code moved. The CSV tables the figures draw from are tracked, and
`tests/test_benchmark_artifacts.py` re-derives the published Pd values from the per-frame
counts, so a stale table fails the suite instead of passing quietly.

### Reading the results

Six things the numbers above actually say. All of them are re-derivable from
`results/summary.csv`.

1. **The transition is real, not saturated.** Every detector moves from Pd = 0.000 to
   Pd = 1.000 inside a ~10 dB window of input SNR (peak SNR ~9 dB to ~26 dB). This was worth
   checking: a sweep over positive input SNR would have produced Pd = 1.000 in every cell.
2. **3 dB of threshold offset is the whole difference between two of the detectors.**
   `cfar_numpy_go` (+12 dB, hard-coded) and `cfar_custom_datasetv8` (+15 dB from the config)
   are the same GO-CFAR on the same dB map. With clutter off, the 12 dB version reaches
   Pd = 1.000 at -30 dB input while the 15 dB version needs -25 dB — about 5 dB of extra
   sensitivity — and pays for it with **~5 false alarms in every single frame**, where the
   15 dB version has exactly zero across all 128 frames.
3. **`cfar_advanced_go` is the most conservative of the three as shipped.** Despite deriving
   its threshold from `pfa=1e-5` in the correct linear-power domain, its `min_snr_db=6.0`
   post-filter plus connected-component pruning make it the last to detect (Pd = 0.000 until
   -25 dB input) — and it degrades worst under `clutter_strong`, reaching only Pd = 0.500 at
   -15 dB where `cfar_custom_datasetv8` reaches 0.938.
4. **The false-alarm rate is independent of SNR, as a CFAR's should be.** `cfar_numpy_go` sits
   at 4.75-5.81 false alarms per frame across a 35 dB span of input SNR. CFAR thresholds are
   relative to a local noise estimate, so they are scale-invariant; the noise realization is
   statistically identical at every SNR point and only the target amplitude moves. Seeing this
   flat is a consistency check on the harness, not a defect.
5. **The accuracy numbers are bin-limited, not noise-limited.** At high SNR the measured range
   RMSE is 0.040 m against a quantization floor of 0.043 m, and velocity RMSE is 0.136 m/s
   against a floor of 0.135 m/s. None of these detectors does sub-bin peak interpolation: they
   report the bin index converted to metres. So the reported accuracy is a property of the FFT
   grid and the reporting convention, and better thresholding cannot improve it — peak
   interpolation (or a model that regresses position) is what would.
6. **`apply_realistic_effects` does essentially nothing at its shipped setting** — see the
   scenario section above for the quantified version.

Statistical caveat: 16 frames per point means Pd is quantized to 1/16 = 0.0625, and a measured
Pd = 1.000 has a two-sided 95% lower confidence bound of about 0.79. Read the transition
region, not the endpoints.

Machine-readable outputs:

* `results/summary.csv` — one row per (detector, scenario, SNR point) with every metric.
* `results/frames.csv` — per-frame TP/FP/FN, for re-deriving any aggregate.
* `results/results.json` — the above plus radar parameters, detector configuration,
  association gate, quantization floor, timing, environment and the shim report.
* `results/summary_table.md` — the Markdown table injected above.

## How to run

Requirements: Python 3.9+, `numpy`, `scipy`, `tqdm`. `matplotlib` is optional (figures only).
**torch is not required and is not installed** — see "Why a shim" below.

```bash
python -m benchmarks.run_benchmark                    # default sweep, writes results/
python -m benchmarks.run_benchmark --update-readme    # also refresh the table above
python -m benchmarks.run_benchmark --quick            # ~15 s smoke run, not a result
python -m benchmarks.run_benchmark --config config1 --trials 8 --no-figures
python -m benchmarks.run_benchmark --help             # every knob
```

Useful flags: `--config` (any FMCW key of `RADAR_CONFIGS`), `--snr-db`, `--trials`, `--seed`,
`--scenarios`, `--gate-range-bins`, `--gate-doppler-bins`, `--pfa`, `--mtd`, `--max-targets`.

Determinism: the repo's simulation calls `np.random.randn` and `random.uniform` on the global
RNGs, which take no injected generator, so the harness seeds both globals per frame from a
`SeedSequence` keyed on `(base_seed, trial)` for the scene and `(base_seed, trial, snr_index)`
for the noise. Consequences, both covered by tests: reruns with the same `--seed` reproduce
byte-identical range-Doppler maps, and **the SNR sweep is paired** — every SNR point sees the
same set of physical scenes, only the noise realization changes.

Tests:

```bash
python -m pytest tests/test_benchmark_metrics.py -q
python -m ruff check benchmarks tests
```

## Why a shim (`benchmarks/repo_shim.py`)

`AIRadarLib/radar_det.py` contains three pure numpy/scipy CFAR detectors and one torch one,
but does `import torch`, `import torch.nn.functional as F` and `import matplotlib.pyplot` at
module scope — so **none** of the numpy detectors can be imported without a multi-gigabyte
torch install. `AIradar_datasetv8.py` is the same: its FMCW signal model is pure numpy, but the
module imports torch, h5py and matplotlib at the top.

Rather than patch the repo (this directory is additive only) or install torch to reach numpy
code, the shim installs minimal placeholder modules for whichever heavy dependencies are
*absent*, imports the repo modules, then removes the placeholders from `sys.modules` again.
Anything genuinely installed is never stubbed. Two guards keep this from becoming a lie:

* `assert_no_stub_dependency` inspects the bytecode of every benchmarked function and fails if
  it references `torch`, `F`, `h5py`, `plt`, `cm` or `cv2`. A test asserts it *does* fire on
  `cfar_2d_pytorch`.
* The shim report (which modules were faked) is recorded in `results/results.json`.

One placeholder detail worth knowing: `torch.Tensor` must exist on the stub, because scipy's
array-API compatibility layer resolves `torch.Tensor` whenever a module named `torch` is
importable, and a bare `ModuleType` makes every scipy call raise `AttributeError`.

`AIRadarDataset` is never instantiated by the benchmark, because its `__init__`
unconditionally generates *and* HDF5-saves an entire dataset. Instead
`benchmarks/scenarios.py` builds a `RadarParams` holder that mirrors the `__init__`
derivation and calls the repo's unbound methods against it. A test constructs one real
`AIRadarDataset` (with `save_dataset` stubbed) for three configs and asserts every derived
attribute and axis matches, so the mirror cannot drift.

## Adding a detector (the plug-in point)

`benchmarks.detectors.Detector` is the extension point. Supply a `run` callable with the
signature `(frame, params) -> list[benchmarks.metrics.Point]`:

```python
from benchmarks.detectors import Detector
from benchmarks.metrics import Point

def run_my_model(frame, params):
    # frame.rd_map_detector_input : float64 [1, 2, num_doppler, num_range]  (real, imag)
    # frame.rd_map_db             : float64 [num_doppler, num_range]        (repo's dB map)
    # frame.rd_map_complex        : complex [num_doppler, num_range]
    peaks = my_model(frame.rd_map_detector_input)          # e.g. a torch RadarNet
    return [
        Point(
            range_bin=r,
            doppler_bin=d,
            range_m=float(params.range_axis[r]),
            velocity_mps=float(params.velocity_axis[d]),
        )
        for d, r in peaks
    ]

Detector(
    name="radarnet_v1",
    source="AIRadar/AIRadarLib/modeling_RadarNet.py",
    description="learned detector, checkpoint xyz",
    run=run_my_model,
    range_gate_low_m=1.0,
)
```

Append it to the list returned by `build_detectors` (or add a registry hook). Nothing in
`metrics.py`, the association rule, the aggregation or the reporting needs to change — a
learned detector is scored by exactly the same rule as the CFAR baselines, which is the point
of building the classical baseline first.

## What the first study does and does not establish

The three detectors were compared at thresholds solved for one measured false-alarm
density, then scored on the same 224 frames (2 scenarios x 7 SNR points x 16 trials).

* They detected the true target in the **same frames in 220 of 224** cases (98%).
* All three per-frame counts -- true positives, false positives and false negatives --
  matched in only **82 of 224** (37%): the detectors disagree about *false alarms* frame
  by frame, which the equal-rate calibration equalises on average rather than per frame.
* Because Pd depends only on the true positives, a paired bootstrap of the Pd = 0.50
  shift between any two of them returns exactly zero in **100%** of 2000 resamples. That
  is a degenerate statistic, not a confidence bound: with no disagreement in the quantity
  being resampled there is nothing to bootstrap, so **no upper bound on the difference is
  available from this data**.

What this establishes: on a homogeneous-noise scene, at this operating point, the choice
between these three implementations does not change which targets are found, so a table
that ranks them at their shipped thresholds is measuring the thresholds. What it does not
establish: that the three are equivalent in general. Detection theory predicts they
should agree here -- cell-averaging, greatest-of and smallest-of CFAR coincide when the
reference window is homogeneous, and they are designed to differ at a clutter edge, which
this scene does not contain. Bounding the difference would need a scene where they
disagree.

## What this does NOT measure

Be blunt about the scope; this is one slice, not a paper.

* **No learned model is evaluated.** torch is not installed in this environment, so the
  torch-based `cfar_2d_pytorch`, `modeling_RadarNet`, `modeling_TimeNet`,
  `modeling_transformer` and every `AIradar_train*.py` pipeline are untouched. The plug-in
  point above is tested; the model behind it is not.
* **No training, no learning curves, no generalization claim.** Nothing is fitted.
* **Simulation only.** Every number comes from the repo's own FMCW model. No CN0566 / Pluto
  capture, no real-world data, no measured hardware impairments. `config_cn0566` (which enables
  the hardware-impairment model, 2 RX and the array factor) is *not* part of the default sweep.
* **Single RX after coherent summation.** `simulate_fmcw_signal` sums the RX channels before
  the FFT, so the range-Doppler map has one channel. Angle-of-arrival estimation is therefore
  disabled and **no angular accuracy is measured**, even though the detectors can estimate AoA.
* **FMCW only.** The OTFS path (`simulate_otfs_signal`, `config_otfs`) is not covered.
  `RadarParams` refuses non-FMCW configs rather than guessing at their axes.
* **One target per frame by default.** The SNR axis is only unambiguous with a single target,
  because `simulate_fmcw_signal` normalizes noise against the *total* signal power, so with
  several targets of different RCS "the SNR" is not any one target's SNR. Multi-target
  resolution, target masking and merged-detection behaviour are therefore **not** measured.
  `--max-targets N` runs it, but read the Pd column as a scene-average, not a per-target SNR
  curve.
* **The false-alarm numbers are peak-level, not per-cell.** All three detectors apply
  non-maximum suppression (and `cfar_advanced_go` additionally collapses each connected
  component to one peak), so the reported rate is a density of surviving peaks. It cannot be
  compared against a CFAR design `Pfa`, and `--pfa` therefore changes `cfar_advanced_go`'s
  threshold without the output rate tracking it.
* **No detector tuning.** Each detector runs with the parameters the repo ships
  (`RADAR_CONFIGS[...]['cfar_params']`, `min_snr_db=6.0`, `pfa=1e-5`). The comparison is
  "as shipped", not "at its best". A fair ROC would sweep each detector's threshold to a
  common false-alarm rate; this slice does not.
* **Small sample size.** 16 frames per point means a Pd of 1.000 has a 95% lower confidence
  bound near 0.8. Treat the transition region, not the endpoints, as the signal.
* **CPU single-process, one machine.** The timing line is a runtime budget check, not a
  throughput benchmark, and no attempt is made to compare against another implementation.

## Repo issues found while building this

Measured, not inferred. None of these are fixed here (this directory is additive only).

1. `AIRadarLib/radar_det.py` imports `torch` and `matplotlib.pyplot` at module scope, which
   makes its three pure-numpy CFAR detectors unimportable in a numpy-only environment.
   Same for `AIradar_datasetv8.py` (torch, h5py, matplotlib) around a pure-numpy signal model.
2. `AIradar_datasetv8.py` imports `cfar_2d_numpy` from `AIRadarLib.radar_det` and never uses
   it. It cannot be used as written: `cfar_2d_numpy` wants a complex `[num_rx, 2, doppler,
   range]` map, but `simulate_fmcw_signal` only ever returns the dB magnitude map.
3. `cfar_2d_detect` (the first detector in `radar_det.py`) unpacks
   `range_idxs, doppler_idxs = np.where(detections)` on a `[doppler, range]` array — the two
   index arrays are swapped, so its range and velocity outputs are transposed. It is not
   benchmarked here for that reason. `cfar_2d_numpy` and `cfar_2d_advanced` get this right.
4. `cfar_2d_numpy` takes no false-alarm-rate argument; its threshold is a hard-coded
   `noise_est + 12` dB. `cfar_2d_detect` has a `rate_fa=1e-5` parameter that is never read —
   its threshold is also hard-coded at +12 dB.
5. `cfar_2d_numpy` and `_cfar_2d_custom` average the **dB** map to estimate the noise level.
   CA/GO-CFAR is derived for averaging in the linear power domain; averaging in dB estimates a
   geometric rather than arithmetic mean, so the threshold does not correspond to the nominal
   false-alarm rate. `cfar_2d_advanced` does it correctly in linear power.
6. `AIRadarDataset._evaluate_metrics` computes match distance as
   `sqrt(delta_range^2 + delta_velocity^2)` with a threshold of `3.0` — adding metres to
   metres-per-second. It also reports mean *absolute* error under the name
   `mean_range_error`, and there is no Pd, no false-alarm rate and no SNR sweep. This harness
   does not use it.
7. `AIRadarDataset.__init__` unconditionally calls `generate_dataset()` and then
   `save_dataset()`, so merely constructing the object writes an HDF5 file. There is no
   lazy/in-memory mode.
8. `apply_realistic_effects=True` is close to a no-op at the shipped clutter intensity
   (measured -23 dB clutter-to-target RCS power; ~0.03 dB mean perturbation of the RD map),
   because the clutter RCS range (-50..-20 dBsm) sits far below the primary-target RCS range
   (+5..+30 dBsm).
9. `AIradar_model_comparison.py` -- the only script in the repo that sets out to compare
   detectors and produce ROC/PR curves -- cannot be imported. `AIradar_transformer` does
   not exist; `AImodels_joint` exists under `deeplearning/` but is not on the `sys.path`
   the script builds, and does not define the two names it imports from it. It has never
   been runnable as committed.
10. `self.range_resolution` is set twice in `__init__`: first as the FFT bin spacing
   `c*fs/(2*slope*zero_pad)` used to build `range_axis`, then overwritten with the Rayleigh
   resolution `c/(2B)`. With `zero_pad_factor=2` these differ by exactly 2x. Any code that
   uses `range_resolution` as a bin spacing is off by that factor.
   `AIRadarDataset.cfar_detection` correctly uses `range_axis[1] - range_axis[0]` instead;
   this harness does the same.

---

# Second study: what actually moves the detection curve

The first study above ends in a null result: at a common **measured** false-alarm
density the three shipped CFAR detectors produce the same detections on this scene, and
the ranking they appear to have at their shipped settings is an artefact of three
different thresholds. That answers "which detector should I use" with "on homogeneous
noise it does not matter", and immediately raises the question this second study
measures:

> If the detector choice does not move the detection curve, what does?

Everything below is generated by one command, from the same simulator, the same
association rule and the same calibration code:

```
python -m benchmarks.run_axes --update-readme
```

## The method, in one paragraph

Every configuration compared here is **re-calibrated on its own target-free frames
before its Pd is read**. Comparing configurations at fixed threshold parameters would
repeat exactly the error the first study diagnosed, so the calibration stage of
`benchmarks/calibration.py` is reused unchanged and each configuration's threshold is
*solved for*, never set.

Three things differ from the first study, each for a stated reason:

1. **The common operating point is a false-alarm rate per frame, not per cell.** Two
   axes (range zero-padding, chirp count) change how many cells the range-Doppler map
   has while covering the same physical range/velocity volume. Equalising the per-cell
   density would silently hand the finer grid twice the false alarms per frame, which is
   the quantity a tracker downstream actually pays for. The existing per-cell solver is
   still what does the work: the target it is given is
   `target_fa_per_frame / eligible_cells`, so the cell count cancels. The rate itself is
   the first study's headline point (1e-4 per cell on the baseline grid) expressed per
   frame, so the baseline configuration sits at exactly the operating point the first
   study reported.
2. **The SNR grid is 1.5 dB instead of 3 dB, and wider.** A 3 dB grid can locate a Pd
   curve but cannot resolve a shift of it. The grid spans 18 dB so that a configuration
   gaining or losing several dB still has its Pd = 0.5 crossing bracketed rather than
   extrapolated.
3. **Calibration is disjoint from evaluation on both counts.** The first study separated
   the noise realisations; this one also draws the calibration scenes from a separate
   block of trial indices, so a calibration frame shares neither a scene nor a noise
   draw with any evaluation frame.

Two numbers are reported per configuration, because they answer different questions:

* **Pd at a fixed SNR** -- the detection-rate change at one operating point.
* **the dB shift at Pd = 0.5** -- the SNR at which the baseline reaches Pd = 0.5 minus
  the SNR at which the configuration reaches it, by linear interpolation of the measured
  curve (`benchmarks/sensitivity.py`, unit-tested). Positive means the configuration
  detects the same target further down. This is the number that compares against a link
  budget, and it is the one to read.

Both carry a **paired** bootstrap interval. Every configuration is measured on the same
physical scenes with the same noise draws -- verified by a hash of the drawn ground
truth that the runner asserts is identical across all configurations -- so
scene-to-scene difficulty cancels out of a difference. That is what makes a 16-frame
budget able to resolve anything at all; see "the error bar" in the generated section.

## The axes, and what could not be reached

Checked against the repo before committing to the list.

| axis | reachable? | how |
|---|---|---|
| CFAR guard cells | yes | `cfar_params['num_guard']`, read by all three detectors |
| CFAR training cells | yes | `cfar_params['num_train']` |
| NMS kernel | yes | `cfar_params['nms_kernel_size']`; note the repo's `if nms_kernel_size > 1` means 1 disables NMS entirely |
| CFAR averaging (CA/GO/SO) | partly | `cfar_2d_numpy` and `cfar_2d_advanced` take `method`; **`_cfar_2d_custom` hard-codes GO**, so this axis is measured on `cfar_2d_numpy` against its own GO baseline |
| range zero-padding | yes | `AIRadarDataset.__init__`'s `zero_pad_factor`, mirrored by `RadarParams` |
| coherent integration (chirps) | yes | `N_chirps` in the `RADAR_CONFIGS` entry; the Doppler FFT length follows it |
| non-coherent integration | not in the repo | added in `benchmarks/axes.py` as a power-map average over looks at the same scene. Nothing under `AIRadar/` is touched |
| `--mtd` | yes, but it is not integration | `_cfar_2d_custom(mtd=True)` only *discards detections* with `abs(velocity) < 1.0`. It is a rejection filter, not Doppler integration, so it is swept as one |

Dropped, with reasons:

* **`cfar_2d_advanced`'s `min_snr_db` post-filter.** It adds a constant number of dB to
  the effective threshold and nothing else, so at a common calibrated false-alarm rate it
  is a no-op by construction -- the calibration removes exactly what it adds. Sweeping it
  would re-measure the first study's artefact, not a property of the detector.
* **`cfar_2d_advanced`'s `suppress_zero_doppler_width`.** Only one of the three detectors
  has it, and it does the same job as `mtd` (blank the zero-Doppler region), which is
  swept on the detector the dataset pipeline actually calls.
* **The detector input transform (dB-magnitude vs linear power).** That *is* the
  difference between `cfar_2d_numpy` / `_cfar_2d_custom` and `cfar_2d_advanced`, and the
  first study already measured it at a common false-alarm rate: null.
* **Everything the first study could not reach either** -- clutter (this study runs
  `clutter_off` only), angle of arrival, OTFS, learned detectors, multi-target scenes.

## Results

<!-- BEGIN GENERATED AXES -->

Radar config `config_phaser` (Phaser_10GHz_DevKit): baseline RD map 64 x 667 bins, range bin 0.150 m, velocity bin 0.468 m/s.

32 frames per SNR point, 1 target per frame, base seed 20260822, SNR referenced to **target** power, clutter disabled. Association gate held fixed in physical units at +/-0.300 m and +/-0.468 m/s and converted to bins per configuration, because two axes change the bin grid.

**The common operating point.** Every configuration below is calibrated on its own target-free frames to the same measured rate of **4.243 false alarms per frame** (= 1e-04 per cell on the baseline 64 x 667 grid, which is the first study's headline point). A rate per frame rather than per cell, because the zero-padding and chirp-count axes change the number of cells while covering the same physical volume; per-cell equality would hand the finer grid more false alarms per frame. 20 target-free frames per configuration give 85 expected false-alarm events at that rate.

**Did the calibration hold?** At the bottom of the sweep (-45.0 dB), where nothing is being detected and the false-alarm count is not reduced by matched detections, the configurations realise 3.62 to 5.16 false alarms per frame (mean 4.14) against the 4.24 they were solved for. That residual is the sampling error of solving a threshold on 20 target-free frames; converted through each calibration curve's own slope it is at most 0.09 dB of threshold (mean 0.03 dB), which is the resolution at which any claim below should be read.

**How to read the result columns.** `FA/frame at floor` is the check above, per row. `Pd@-36.0 dB` is the detection rate at the fixed SNR point nearest the baseline's Pd = 0.50 crossing, and `Pd@-27.0 dB` is the top of the sweep, where a configuration that throws targets away shows a ceiling below 1. `shift` is the dB of sensitivity the configuration buys: the SNR at which the baseline reaches Pd = 0.50 minus the SNR at which this configuration reaches it, so **positive means it detects the same target further down**. Intervals are 95% paired-bootstrap intervals over 2000 resamples of the 32 scenes.

### What the measurement says

**Read this first: the scene.** Every number below is measured on `clutter_off` -- one non-fluctuating point target in homogeneous AWGN. That is precisely the regime in which the CFAR's own parameters are *designed* to be irrelevant: guard cells exist to keep an extended or nearby return out of the noise estimate, and the greatest-of/smallest-of averaging modes exist to hold the false-alarm rate across a clutter edge. Homogeneous noise contains neither. So "the CFAR parameters are flat" is established *for this scene*, and it is not evidence that they can be set carelessly in the scene the shipped pipeline actually runs -- `apply_realistic_effects=True` turns clutter on. Extending these axes to a clutter scenario is the obvious next study and is not done here.


**Marginal rows.** `range_zero_padding` = 1 (+0.36 dB [+0.018, +1.73]) clears zero by less than 0.10 dB. The equal-false-alarm audit puts each configuration within about 0.09 dB of the rate it was solved for, so a margin that small sits inside the calibration residual. Treat these as suggestive rather than established; confirming one would need a larger frame budget and more calibration frames.

**Against detection theory.** The integration axes have a closed-form prediction, so they double as a check on everything upstream of them:

| change | prediction | predicted | measured | difference |
|---|---|---|---|---|
| 64 -> 32 chirps | 10 log10(N) | -3.01 dB | -2.62 dB | +0.39 dB |
| 64 -> 128 chirps | 10 log10(N) | +3.01 dB | +2.90 dB | -0.11 dB |
| 2 non-coherent looks | Albersheim | +2.33 dB | +2.76 dB | +0.43 dB |
| 4 non-coherent looks | Albersheim | +4.54 dB | +4.67 dB | +0.12 dB |
| 8 non-coherent looks | Albersheim | +6.61 dB | +6.43 dB | -0.17 dB |

Every row lands within 0.43 dB of its prediction. Albersheim's approximation is for a non-fluctuating target in a linear detector, which is the target model this simulator generates, and is evaluated only inside the range it is stated for. A harness with a systematic error in the simulator, the calibration or the metric would not reproduce these numbers, so this is the strongest external evidence here that the rest of the table means what it says. It is a check on the measurement chain, not a validation of the simulator against hardware -- nothing in this study touches a radio.
**4 of the 8 axes move the curve** by more than their own 95% interval (`cfar_training_cells`, `range_zero_padding`, `coherent_chirps`, `noncoherent_looks`); the other 4 do not at this budget (`cfar_guard_cells`, `nms_kernel`, `mtd_filter`, `cfar_averaging`).

**Doubling the dwell time, two ways.** Doubling the chirp count -- coherent integration, one detection per frame as before -- buys +2.90 dB [+1.31, +4.25]. Spending the same extra dwell on a second look integrated non-coherently buys +2.76 dB [+1.61, +4.06].

The intervals overlap, so this study cannot rank the two at equal dwell; it can only say that both are worth more than every other axis it swept. The non-coherent route needs no change to the waveform at all -- it is arithmetic on range-Doppler maps the pipeline already computes -- while the coherent route changes the Doppler grid and the frame rate.

**What "flat" means here.** The largest movement anywhere among the flat axes is `num_guard` = 8 at +0.19 dB [+0.00, +1.62], an interval that spans zero; resolving a difference that small at this operating point would need about 114 frames per SNR point instead of 32. Read those rows as "this study cannot distinguish these settings", not as "these settings are identical" -- the upper end of each interval is what an unmeasured effect could still be worth.

**The moving-target filter.** `mtd=True` -- which the dataset pipeline enables whenever `apply_realistic_effects` is set -- moves the Pd = 0.50 crossing by -0.17 dB [-1.50, +0.30], and caps Pd at 0.906 at the top of the sweep against 1.000 without it, because 3 of the 32 drawn targets are slower than the 1 m/s it discards. It buys no sensitivity at this operating point and costs every slow target.

**Non-maximum suppression is not about detection, it is about counting.** Disabling it (`nms_kernel_size=1`) does not move the Pd curve (-0.07 dB [-0.75, +0.00]), but over the swept frames it produces +1.84 false alarms per frame relative to the shipped kernel at the same target-free rate -- the target's own main lobe, reported as several detections instead of one.

### Summary: which axis moves the curve

Ranked by **span**: the dB between the best and the worst value swept on that axis. A large span means the knob matters -- whether or not the shipped value can be improved on, getting it wrong costs that many dB. All shifts are relative to the axis's own baseline value.

| axis | knob | baseline | best value (shift dB) | worst value (shift dB) | span (dB) | any shift resolved? | cost |
|---|---|---|---|---|---|---|---|
| `coherent_chirps` | `N_chirps` | 64 | 128 (+2.90) | 32 (-2.62) | 5.53 | yes | simulation and detection cost scale with N_chirps; dwell time doubles per doubling |
| `noncoherent_looks` | `looks` | 1 | 8 (+6.43) | 2 (+2.76) | 3.67 | yes | L simulated frames per detection; detector cost unchanged |
| `cfar_training_cells` | `num_train` | 10 | 6 (+0.12) | 2 (-1.50) | 1.62 | yes | num_train=2 runs ~5x faster than num_train=10; num_train=16 ~1.9x slower |
| `range_zero_padding` | `zero_pad_factor` | 2 | 1 (+0.36) | 4 (-0.25) | 0.62 | yes | detector cost is proportional to the number of range bins |
| `cfar_guard_cells` | `num_guard` | 4 | 8 (+0.19) | 0 (+0.00) | 0.19 | no | window grows as (2*(train+guard)+1)^2; guard=12 costs ~2.4x the baseline convolution |
| `nms_kernel` | `nms_kernel_size` | 5 | 3 (+0.00) | 1 (-0.07) | 0.07 | no | a maximum filter over the whole map; negligible next to the CFAR convolution |
| `cfar_averaging` | `method` | GO | CA (+0.06) | SO (+0.02) | 0.05 | no | identical (the same convolutions, combined differently) |
| `mtd_filter` | `mtd` | off | on (-0.17) | on (-0.17) | 0.00 | no | free (a filter on the detection list) |

### `cfar_guard_cells` -- `num_guard`

Do the guard cells cover the target's own main lobe? With a Hann range window and zero_pad_factor=2 the main lobe is about +/-4 range bins wide, so a guard block narrower than that feeds target energy into the noise estimate and the detector masks itself.

Cost: window grows as (2*(train+guard)+1)^2; guard=12 costs ~2.4x the baseline convolution. Baseline value: `4`.

| num_guard | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| 0 | 9.68 | 4.28 | 0.406 | -0.031 [-0.094, +0.000] | 1.000 | -35.44 | +0.00 [-0.75, +0.70] | not resolved (95% CI [-0.75, +0.70] dB) |
| 2 | 9.66 | 4.41 | 0.406 | -0.031 [-0.094, +0.000] | 1.000 | -35.55 | +0.04 [-0.43, +1.00] | not resolved (95% CI [-0.43, +1.00] dB) |
| **4** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 6 | 9.61 | 4.12 | 0.406 | -0.031 [-0.094, +0.000] | 1.000 | -35.50 | +0.00 [-0.50, +0.83] | not resolved (95% CI [-0.50, +0.83] dB) |
| 8 | 9.61 | 4.00 | 0.469 | +0.031 [+0.000, +0.094] | 1.000 | -35.81 | +0.19 [+0.00, +1.62] | not resolved (95% CI [+0.00, +1.62] dB) |
| 12 | 9.59 | 4.34 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.70 | +0.12 [+0.00, +1.31] | not resolved (95% CI [+0.00, +1.31] dB) |

### `cfar_training_cells` -- `num_train`

Classical CFAR loss: a noise estimate averaged over N cells needs a higher threshold than a known noise level for the same false-alarm rate. The shipped num_train=10 gives 580 training cells per GO branch, where that loss should already be negligible -- so this axis is a test of whether the shipped value is buying anything for its cost.

Cost: num_train=2 runs ~5x faster than num_train=10; num_train=16 ~1.9x slower. Baseline value: `10`.

| num_train | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| 2 | 9.77 | 4.28 | 0.219 | -0.219 [-0.375, -0.094] | 1.000 | -34.00 | -1.50 [-3.25, -0.38] | loss |
| 4 | 9.65 | 4.41 | 0.312 | -0.125 [-0.250, -0.031] | 1.000 | -35.18 | -0.25 [-1.25, +0.30] | not resolved (95% CI [-1.25, +0.30] dB) |
| 6 | 9.62 | 4.28 | 0.438 | +0.000 [-0.094, +0.094] | 1.000 | -35.67 | +0.12 [-0.31, +1.25] | not resolved (95% CI [-0.31, +1.25] dB) |
| **10** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 16 | 9.61 | 4.25 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.67 | +0.10 [+0.00, +1.20] | not resolved (95% CI [+0.00, +1.20] dB) |

### `nms_kernel` -- `nms_kernel_size`

Non-maximum suppression thins the peak list, so it lowers the measured false-alarm rate and buys threshold back. 1 disables it entirely (the repo's `if nms_kernel_size > 1` guard), which makes every threshold crossing a detection.

Cost: a maximum filter over the whole map; negligible next to the CFAR convolution. Baseline value: `5`.

| nms_kernel_size | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| 1 | 9.70 | 3.97 | 0.406 | -0.031 [-0.094, +0.000] | 1.000 | -35.36 | -0.07 [-0.75, +0.00] | not resolved (95% CI [-0.75, +0.00] dB) |
| 3 | 9.68 | 4.00 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.50 | +0.00 [+0.00, +0.00] | not resolved (95% CI [+0.00, +0.00] dB) |
| **5** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 7 | 9.67 | 4.00 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.50 | +0.00 [+0.00, +0.00] | not resolved (95% CI [+0.00, +0.00] dB) |
| 9 | 9.67 | 4.00 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.50 | +0.00 [-0.02, +0.00] | not resolved (95% CI [-0.02, +0.00] dB) |
| 15 | 9.67 | 4.06 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.50 | +0.00 [-0.02, +0.00] | not resolved (95% CI [-0.02, +0.00] dB) |

### `range_zero_padding` -- `zero_pad_factor`

Zero-padding the range FFT adds no information, but it reduces straddle (scalloping) loss when a target falls between bins. It also doubles the cell count, which at a fixed false-alarm rate per frame costs threshold.

Cost: detector cost is proportional to the number of range bins. Baseline value: `2`.

| zero_pad_factor | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| 1 | 9.27 | 4.28 | 0.500 | +0.062 [+0.000, +0.156] | 1.000 | -36.00 | +0.36 [+0.02, +1.73] | gain |
| **2** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 4 | 9.84 | 3.97 | 0.344 | -0.094 [-0.188, +0.000] | 1.000 | -35.17 | -0.25 [-1.33, +0.29] | not resolved (95% CI [-1.33, +0.29] dB) |

### `coherent_chirps` -- `N_chirps`

Coherent integration: the Doppler FFT sums N_chirps samples in phase, so the target-to-noise ratio in the map should rise 3 dB per doubling. The dwell time rises with it -- compare against noncoherent_looks, which spends the same extra dwell without phase coherence.

Cost: simulation and detection cost scale with N_chirps; dwell time doubles per doubling. Baseline value: `64`.

| N_chirps | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| 32 | 9.19 | 5.16 | 0.062 | -0.375 [-0.562, -0.188] | 1.000 | -32.88 | -2.62 [-3.59, -1.09] | loss |
| **64** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 128 | 9.96 | 3.62 | 0.812 | +0.375 [+0.125, +0.594] | 1.000 | -38.33 | +2.90 [+1.31, +4.25] | gain |

### `noncoherent_looks` -- `looks`

Non-coherent integration: average the power maps of L successive frames of the same scene, then detect once. Costs L times the dwell, exactly like doubling the chirp count -- so the two axes are directly comparable per unit of time spent.

Cost: L simulated frames per detection; detector cost unchanged. Baseline value: `1`.

| looks | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| **1** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| 2 | 6.30 | 4.28 | 0.844 | +0.406 [+0.250, +0.594] | 1.000 | -38.25 | +2.76 [+1.61, +4.06] | gain |
| 4 | 3.99 | 4.31 | 1.000 | +0.562 [+0.405, +0.719] | 1.000 | -40.18 | +4.67 [+3.78, +6.19] | gain |
| 8 | 2.33 | 4.25 | 1.000 | +0.562 [+0.405, +0.719] | 1.000 | -42.00 | +6.43 [+5.38, +7.76] | gain |

### `mtd_filter` -- `mtd`

The dataset pipeline turns `_cfar_2d_custom`'s moving-target filter on whenever apply_realistic_effects is set. It discards every detection with |v| < 1 m/s, which removes false alarms near zero Doppler -- and also removes any real target that happens to be slow.

Cost: free (a filter on the detection list). Baseline value: `off`.

| mtd | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| **off** | 9.67 | 4.00 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| on | 9.63 | 3.94 | 0.375 | -0.062 [-0.156, +0.000] | 0.906 | -35.25 | -0.17 [-1.50, +0.30] | not resolved (95% CI [-1.50, +0.30] dB) |

### `cfar_averaging` -- `method`

GO-CFAR takes the larger of the two training-branch means, which raises the threshold to protect against clutter edges. In homogeneous noise that protection is a loss. Measured on cfar_2d_numpy, the one shipped detector whose `method` argument is reachable (`_cfar_2d_custom` hard-codes GO).

Cost: identical (the same convolutions, combined differently). Baseline value: `GO`.

| method | calibrated thr (dB) | FA/frame at floor | Pd@-36.0 dB | dPd [95% CI] | Pd@-27.0 dB | SNR at Pd=0.50 (dB) | shift (dB) [95% CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| **GO** | 9.67 | 3.97 | 0.438 | -- | 1.000 | -35.50 | -- | baseline |
| CA | 9.72 | 4.03 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.62 | +0.06 [+0.00, +1.02] | not resolved (95% CI [+0.00, +1.02] dB) |
| SO | 9.83 | 4.31 | 0.438 | +0.000 [+0.000, +0.000] | 1.000 | -35.57 | +0.02 [+0.00, +0.60] | not resolved (95% CI [+0.00, +0.60] dB) |

### Calibrated thresholds and what they cost

Every row is a solve on target-free frames, so the threshold column is a *result*, not a setting. A configuration that produces fewer peaks per frame for the same threshold gets its threshold lowered until the rate matches, which is where part of its gain comes from.

| config | detector | knob | calibrated knob value | effective thr (dB) | status |
|---|---|---|---|---|---|
| `cfar_custom_datasetv8__train10_guard0_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.18500 | 9.68 | interpolated |
| `cfar_custom_datasetv8__train10_guard12_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.09513 | 9.59 | interpolated |
| `cfar_custom_datasetv8__train10_guard2_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.16640 | 9.66 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms15_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.17463 | 9.67 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms1_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.20468 | 9.70 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms3_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.18215 | 9.68 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp1_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 11.77288 | 9.27 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc128_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.46375 | 9.96 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc32_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 11.69833 | 9.19 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.18069 | 9.67 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc64_looks1_mtd1` | `cfar_custom_datasetv8` | `threshold_offset` | 12.13796 | 9.63 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc64_looks2_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 8.81028 | 6.30 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc64_looks4_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 6.49358 | 3.99 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp2_nc64_looks8_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 4.84056 | 2.33 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms5_zp4_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.35178 | 9.84 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms7_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.18069 | 9.67 | interpolated |
| `cfar_custom_datasetv8__train10_guard4_nms9_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.18069 | 9.67 | interpolated |
| `cfar_custom_datasetv8__train10_guard6_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.11598 | 9.61 | interpolated |
| `cfar_custom_datasetv8__train10_guard8_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.11426 | 9.61 | interpolated |
| `cfar_custom_datasetv8__train16_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.11460 | 9.61 | interpolated |
| `cfar_custom_datasetv8__train2_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.27999 | 9.77 | interpolated |
| `cfar_custom_datasetv8__train4_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.15421 | 9.65 | interpolated |
| `cfar_custom_datasetv8__train6_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_custom_datasetv8` | `threshold_offset` | 12.12820 | 9.62 | interpolated |
| `cfar_numpy_ca__train10_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_numpy_ca` | `magnitude_warp_exponent` | 0.98121 | 9.72 | interpolated |
| `cfar_numpy_go__train10_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_numpy_go` | `magnitude_warp_exponent` | 0.98535 | 9.67 | interpolated |
| `cfar_numpy_so__train10_guard4_nms5_zp2_nc64_looks1_mtd0` | `cfar_numpy_so` | `magnitude_warp_exponent` | 0.97262 | 9.83 | interpolated |

### The error bar

A Pd measured over 32 frames with one target each has a binomial standard error of 0.201 at Pd = 0.5, and is quantized to 1/32 = 0.0312. That is the error bar on an **absolute** Pd, and it is why no absolute Pd in this study should be read to better than about +/-0.40.

**Where the interval is not a bound.** Pd moves in steps of 1/32, so the shift is a lattice-valued statistic and a resample often reproduces the baseline curve exactly, putting an atom of probability at exactly zero. In 8 of the 24 comparisons that atom holds at least 25% of the resamples (up to 100%): `cfar_training_cells` = 16 (30%), `nms_kernel` = 1 (42%), `nms_kernel` = 3 (100%), `nms_kernel` = 7 (100%), `nms_kernel` = 9 (96%), `nms_kernel` = 15 (96%), `cfar_averaging` = CA (37%), `cfar_averaging` = SO (50%). For those rows a percentile endpoint printed as +0.00 is the atom itself rather than a 2.5% quantile, so it is **not** an upper bound on the effect -- it says the two configurations produce the same detections on these frames, which is a statement about this budget and these scenes. Bounding those axes needs either more frames or a smooth test statistic; neither is done here.

**Multiplicity.** 24 non-baseline comparisons are each reported at a 95% interval and none is corrected for the family, so roughly one in twenty would be expected to exclude zero by chance. Scoring each shift against its own bootstrap standard error and applying a Bonferroni threshold for 24 tests, 5 survive (`coherent_chirps` = 32, `coherent_chirps` = 128, `noncoherent_looks` = 2, `noncoherent_looks` = 4, `noncoherent_looks` = 8) and 2 of the rows this study calls resolved do not (`cfar_training_cells` = 2, `range_zero_padding` = 1). That scoring assumes the shift is approximately normal, which the rows flagged below are not, so treat it as an ordering rather than an exact p-value.

The differences are better resolved than that, because they are paired: every configuration is measured on the same physical scenes and the same noise draws, so scene difficulty cancels. The bracketed intervals are the bootstrap of that paired difference, and they are the numbers to read. Where an interval spans zero the study **cannot resolve that axis** at this budget. The per-point frame budget that would resolve the Pd difference at the reference SNR is a different statistic and is carried in the CSV rather than the verdict.

Total: 26 configurations, 14176 detector calls.

Provenance: everything above depends only on the seeds. Wall-clock timings, the environment and the git state are deliberately kept out of this block and written to `results_axes/axes_run_meta.json` instead, so that two runs of the study produce byte-identical copies of every other artifact.

<!-- END GENERATED AXES -->

Figures (written only when matplotlib is installed): one Pd-vs-SNR figure per axis,
`results_axes/axes_pd_<axis>.png`, plus `results_axes/axes_shift_summary.png` -- every
configuration's dB shift with its bootstrap interval, grey where the interval spans zero.

Machine-readable outputs, all in `benchmarks/results_axes/`:

* `axes_configs.csv` -- one row per (axis, value): calibrated threshold, Pd, shift, interval, verdict.
* `axes_summary.csv` -- one row per (configuration, SNR point) with every metric.
* `axes_calibration.csv` -- every threshold actually measured, including the points the
  bracket search added, so the solve can be audited.
* `axes_frames.csv` -- per-frame detections and false alarms; every aggregate above is
  re-derivable from this file alone.
* `axes_results.json` -- all of the above plus the settings, the ground-truth hash and
  the full analysis. Depends only on the seeds. Written on every run but **not tracked**:
  it is a 0.9 MB superset of the CSVs above, and `--render-from` needs only a fresh copy,
  not a historical one. The figures are untracked for the same reason.
* `axes_run_meta.json` -- wall-clock timings, environment, git state. Deliberately
  separate, so that two runs of the study produce byte-identical copies of everything else.

## What this second study does NOT measure

* **One scenario.** `clutter_off` only. That matters most for the CA/GO/SO axis: GO-CFAR
  exists to protect against clutter edges, and a study in homogeneous noise measures only
  what that protection costs, never what it buys. Do not read the CA row as "switch to
  CA" without re-running it under `clutter_strong`.
* **One target per frame, one radar config, FMCW only, CPU only, torch-free.** Same scope
  as the first study, for the same reasons.
* **Non-coherent integration assumes a frozen scene.** The looks are independent noise
  realisations of the *same* stationary scene: no target motion between looks, no range
  walk, no frame-to-frame registration. A real multi-frame integrator has to solve those,
  so the measured gain is an upper bound on what that dwell buys in practice.
* **Coherent integration changes the Doppler grid.** Doubling `N_chirps` halves the
  velocity bin. The association gate is held fixed in m/s rather than in bins so that this
  does not leak into the Pd column, but the velocity RMSE column is still grid-limited and
  is not comparable across that axis. At 32 chirps the gate cannot be held exactly -- one
  bin is already wider than the requested gate -- so that row is scored with a gate twice
  as wide in m/s as every other row, which if anything flatters it.
* **The "effective threshold (dB)" column is a label, not a measurement.** It comes from
  the closed-form dB-domain bias in `benchmarks/detectors.py`, which assumes a single
  look; for the multi-look rows the true distance above the mean noise power is smaller
  than the label says. Nothing in the comparison depends on it -- the operating point is
  the *measured* false-alarm rate, which the "measured FA/frame" column reports for every
  row.
* **No cost model.** The cost column is a note, not a measurement. Where an axis costs
  dwell time (chirps, looks) that is stated in the axis description; where it costs CPU
  (window size, zero-padding) the per-configuration timings are in `axes_run_meta.json`.

## How to run the second study

```bash
python -m benchmarks.run_axes                      # the full study
python -m benchmarks.run_axes --update-readme      # also refresh the table above
python -m benchmarks.run_axes --quick              # smoke run, not a result
python -m benchmarks.run_axes --axes cfar_guard_cells --workers 4
python -m benchmarks.run_axes --help               # every knob
```

The configurations are independent, so they are measured in a process pool and
`--workers` changes only the wall clock. Results are byte-identical at any worker count,
checked by running the same study at `--workers 1` and `--workers 6` and comparing every
output file.

Budget: the default study takes roughly ten minutes of wall clock on twelve worker
processes, or a bit under two CPU-hours in total. The exact figures for the run that
produced the tables above, including a per-configuration breakdown, are in
`results_axes/axes_run_meta.json`. `--trials 16` roughly halves it, at the cost of
widening every interval by about 40%; `--axes <name>` runs one axis against its own
baseline.

Tests:

```bash
python -m pytest tests/test_benchmark_axes.py -q
python -m ruff check benchmarks tests
```

## File map

```
benchmarks/
  run_benchmark.py   first study: argparse entry point, sweep, CSV/JSON/figure/README output
  run_axes.py        second study: axis sweep, paired bootstrap, CSV/JSON/figure/README output
  axes.py            second study: the axis grid, non-coherent integration, one configuration end to end
  sensitivity.py     second study: Pd -> dB interpolation and its error bars (pure, no repo imports)
  calibration.py     measured Pfa-vs-threshold curves and the solve for a common rate
  scenarios.py       RadarParams mirror + seeded scene and frame generation
  detectors.py       adapters over the three repo CFAR detectors; the plug-in point
  metrics.py         association, Pd/FAR/RMSE aggregation (pure, no repo imports)
  repo_shim.py       torch-free import of the repo modules, with guards
  results/           generated CSV / JSON / Markdown / PNG, first study
  results_axes/      generated CSV / JSON / Markdown / PNG, second study
tests/
  test_benchmark_metrics.py   hand-computed metric cases + repo-fidelity tests
  test_benchmark_axes.py      hand-computed interpolation/bootstrap cases + axis plumbing

There is deliberately no `tests/conftest.py`: both test files put the repository root on
`sys.path` themselves, so they run under a bare `pytest` without colliding with the
conftest that the hardware-free pytest suite introduces on another branch.
```
